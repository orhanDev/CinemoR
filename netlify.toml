[build]
  command = "npm run build"
  publish = "dist"

# SPA routing için redirect kuralları
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "18"
